// Shared types for the Round 1 V2 engine.
// Phase 1 scope: wide aspect only, 3 non-fallback backgrounds, no square/tall.

export type { Round1Engine } from "./config";

export type Round1V2Result = { error?: string };
